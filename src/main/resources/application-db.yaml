spring.jpa:
  database: MYSQL
  show-sql: true

spring.datasource:
  url: jdbc:mysql://localhost:3306/rfch-blog?useUnicode=yes&characterEncoding=UTF-8
  username: root
  password:
spring:
  datasource:
    platform: org.hibernate.dialect.MySQL8Dialect
#TODO: Change it later `update` to `validate`